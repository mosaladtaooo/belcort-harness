# BELCORT Harness v3.1.0 — Verification Augmentation Implementation Plan — Part 1 of 2 (Phases 1-3 — Implementation)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **This is Part 1 of 2.** Part 2 (`-part-2.md`) contains documentation updates (Phase 4) + pre-push audit + canary + release (Phase 5). Complete all 15 implementation tasks here before starting Part 2.

**Goal:** Ship 8 verification-augmentation additions atomically as v3.1.0 — worker readiness signal, axe-core a11y, code-reviewer integration, Stryker mutation, fast-check property, MADR ADR matrix, size-limit budget, and refactor-pattern documentation stub. Predicted net LoC: +460.

**Architecture:** All v3.1 changes are file edits + skill invocations within v3.0's existing 3-agent topology. No new MODE on any agent, no new subagent type, no new commands. Tier 1 (G1-G3) lands first as minimum-shippable; Tier 2 (G4-G6) bundles substantive verification additions; Tier 3 (G7-G8) closes opportunistic + documentation gaps.

**Tech Stack:** Markdown agent prompts (`agents/*.md`), template `.txt` files, manifest YAML, prose command procedures. New ecosystem dependencies: `@axe-core/playwright`, `@stryker-mutator/core` + `@stryker-mutator/vitest-runner`, `@fast-check/vitest`, `size-limit` + `@size-limit/preset-app`. Optional plugin upgraded: `superpowers` (was Optional v3.0; soft-required v3.1+ for Evaluator Step 3a).

**Spec reference:** `docs/superpowers/specs/2026-05-08-belcort-v3.1-verification-augmentation-design.md`.

---

## File Structure

### New files

None. All v3.1 additions edit existing files.

### Modified files (Phase 1-3 — this part)

| Path | Modifications |
|---|---|
| `plugins/harness/agents/planner.md` | Pass 1 WCAG-conformance question (G2); Pass 1 bundle-size budget question (G7); Pass 2 worker_ready_pattern declaration (G1); Pass 2 MADR ADR matrix replacement (G6); RED FLAG row 5 update (G6); V8b self-validation row (G6); 18-point → 19-point header bump |
| `plugins/harness/agents/generator.md` | NEGOTIATE adds property-test-eligibility column (G5); BUILD adds mutation-config + size-limit-config + axe + property test scaffolding (G2/G4/G5/G7); SIMULATE Step 2.6 worker readiness (G1); SIMULATE Step 3 axe per row (G2); RED FLAG table row 7 update (G8) |
| `plugins/harness/agents/evaluator.md` | Step 3 splits into 3a (code-reviewer, G3) + 3b (manual) + 3c (greps); Step 4 adds mutation gate (G4) + size-limit gate (G7) + property-test triviality check (G5) |
| `plugins/harness/skills/harness/SKILL.md` | New § Worker Readiness Patterns (G1); new § Refactor Pattern (G8); Optional Plugins table annotation for superpowers v3.1+ (G3) |
| `plugins/harness/commands/analyze.md` | Tech stack conflicts check gains "every ADR has ≥2 alternatives + scoring matrix" warning (G6) |
| `plugins/harness/commands/doctor.md` | superpowers plugin missing → MAJOR warning (was advisory v3.0; G3) |
| `plugins/harness/templates/features/contract.md.txt` | Mutation Score Target section (G4); Bundle-size Budget section (G7); WCAG Level reference (G2) |
| `plugins/harness/templates/features/proposal.md.txt` | Property-test eligibility column in per-FR table (G5) |
| `plugins/harness/templates/features/simulation-report.md.txt` | a11y column in per-state-transition table (G2) |
| `plugins/harness/templates/evaluator/criteria.md.txt` | Promote line 138 prose to first-class a11y enforcement (G2); add mutation-score + bundle-size dimensions (G4/G7) |

### Modified files (Phase 4-5 — see Part 2)

`docs/anthropic-alignment.md`, `CHANGELOG.md`, `ROADMAP.md`, `README.md`, `plugins/harness/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`.

---

## Phase 1 — Tier 1 (lowest risk, validates baseline)

### Task 1: Worker readiness signal — SIMULATE Step 2.6 + canonical patterns (G1 / A2 part A)

**Files:** Modify `plugins/harness/agents/generator.md`, `plugins/harness/skills/harness/SKILL.md`

- [ ] **Step 1: Insert SIMULATE Step 2.6 in generator.md**

Locate `## MODE: SIMULATE` → `#### Step 2: Start production stack` end. After sub-step 5 (server bind wait), insert before `#### Step 3`:

```markdown
**Sub-step 2.6: Worker readiness signal (v3.1+)**

If a worker process exists (`pnpm worker` started in 2.4) AND
architecture.md or proposal.md declares `worker_ready_pattern`, wait
for the positive readiness signal via Bash `run_in_background: true`:

    timeout 30 bash -c 'until grep -q "<pattern>" logs/worker.log; do sleep 0.5; done'

Substitute `<pattern>` with the declared pattern (e.g., `^WORKER:READY$`).

On match: proceed to Step 3 — and REMOVE the worker-readiness retry policy
from Step 3 (the worker is already warm; retrying masks real bugs per
the existing rule for non-worker rows).

On 30s timeout: kill captured PIDs from sub-steps 2.3 and 2.4, write
`## Infrastructure failure: worker readiness timeout` to
simulation-report.md with verdict NEEDS-REPAIR, exit.

If no `worker_ready_pattern` declared, use the convention-only fallback:
match any of the canonical patterns from SKILL.md § Worker Readiness
Patterns. If none match within 30s, fall back to v3.0's polling behavior
(non-failure — the worker may simply be a silent-idle pattern).
```

- [ ] **Step 2: Add SKILL.md § Worker Readiness Patterns**

After existing `## Test-Fixture Pattern (v3.0+)` (and before `## File Ownership Contract`), add:

```markdown
## Worker Readiness Patterns (v3.1+)

When SIMULATE drives a worker-dependent state transition, it needs to
know when the worker is warm. v3.1 introduces a positive readiness signal
via stdout pattern matching, replacing v3.0's 30s polling.

### Project author's responsibility

Declare in `architecture.md`'s worker subsection:

    worker_ready_pattern: "^WORKER:READY$"

Anchor the pattern (`^...$` or word-boundary `\bREADY\b`) to avoid
false-ready from debug logs containing "ready" as a substring.

### Convention-only fallback

If no `worker_ready_pattern` declared, SIMULATE matches any of these
canonical patterns within 30s:

- `Server listening on`
- `worker ready`
- `[ready]`
- `READY:GO`
- `Database connection established`
- `Queue subscribed`

If none match, falls back to v3.0's 30s polling timeout.

### Why a positive signal

- Eliminates false NEEDS-REPAIR on slow-warming workers (DB pool, JIT,
  embeddings model load)
- Eliminates false-positive successes on warm-but-stale workers (PID leak
  from prior SIMULATE handles state transitions using cached
  pre-migration data)
- Zero turn-burn vs Monitor-based streaming (one completion notification
  regardless of session length)

### Worker stdout piping convention

For the readiness pattern to be observable, the worker's stdout must reach
a file. Standard `package.json`:

    "worker": "tsx scripts/worker.ts 2>&1 | tee logs/worker.log"

Generator BUILD adds this to scaffolding when a worker is declared.
```

- [ ] **Step 3: Verify**

Grep `agents/generator.md` for `Sub-step 2.6: Worker readiness signal` — expect 1 match. Grep `skills/harness/SKILL.md` for `## Worker Readiness Patterns` — expect 1 match.

- [ ] **Step 4: Commit**

```
git add plugins/harness/agents/generator.md plugins/harness/skills/harness/SKILL.md
git commit -m "feat(harness/v3.1/G1): worker readiness signal — SIMULATE Step 2.6 + canonical patterns"
```

---

### Task 2: Worker readiness signal — Planner Pass 2 declaration (G1 / A2 part B)

**Files:** Modify `plugins/harness/agents/planner.md`

- [ ] **Step 1: Locate Pass 2 architecture-emission section**

Grep for `## Architecture` or `Pass 2`'s architecture-emission sub-step.

- [ ] **Step 2: Insert worker_ready_pattern declaration rule**

Within Pass 2's architecture template prose:

```markdown
**Worker readiness signal (v3.1+).** When the architecture includes a
worker process, declare in the worker subsection:

    worker_ready_pattern: "<exact-anchored-regex>"

Examples:
- `worker_ready_pattern: "^WORKER:READY$"` (anchored exact)
- `worker_ready_pattern: "^Server listening on \\d+$"` (anchored canonical)
- `worker_ready_pattern: "\\bDatabase connection established\\b"` (word-boundary)

Anchor the pattern to avoid false-ready from debug logs. SIMULATE matches
against the worker's stdout (piped via `pnpm worker 2>&1 | tee logs/worker.log`).

If no stable readiness signal exists (worker idles silently until a job
arrives), omit the declaration; SIMULATE falls back to canonical-pattern
detection per SKILL.md § Worker Readiness Patterns.
```

- [ ] **Step 3: Verify**

Grep `planner.md` for `worker_ready_pattern` — expect 3+ matches.

- [ ] **Step 4: Commit**

```
git add plugins/harness/agents/planner.md
git commit -m "feat(harness/v3.1/G1): Planner Pass 2 declares worker_ready_pattern in architecture"
```

---

### Task 3: axe-core a11y — Planner Pass 1 WCAG question (G2 / B4 part A)

**Files:** Modify `plugins/harness/agents/planner.md`

- [ ] **Step 1: Locate Pass 1 NFR enumeration end**

- [ ] **Step 2: Insert WCAG-conformance question sub-step**

```markdown
**Sub-step W: Accessibility conformance level (v3.1+)**

When the project has any UI surface (architecture declares a UI layer),
ask via AskUserQuestions:

> "What WCAG conformance level should this project meet?
>   1. AA (recommended default — public-facing apps)
>   2. AAA (strict — government / accessibility-critical)
>   3. A only (internal tools — minimal)
>   4. N/A (no UI — non-applicable)"

Record in `prd.md` under `## Accessibility NFR` as: `WCAG-Level: A | AA | AAA | N/A`.

If `N/A`: SIMULATE Step 3 skips axe-core scans entirely. If `A | AA | AAA`:
SIMULATE runs AxeBuilder with appropriate tags and Evaluator gates Part A
on `impact ≥ serious` violations.

Skip this sub-step if architecture explicitly declares no UI surface.
```

- [ ] **Step 3: Verify**

Grep for `WCAG-Level:` — expect 2-3 matches.

- [ ] **Step 4: Commit**

```
git add plugins/harness/agents/planner.md
git commit -m "feat(harness/v3.1/G2): Planner Pass 1 asks WCAG conformance level for UI projects"
```

---

### Task 4: axe-core a11y — Generator BUILD scaffolding (G2 / B4 part B)

**Files:** Modify `plugins/harness/agents/generator.md`

- [ ] **Step 1: Locate BUILD Phase 1 scaffolding section**

- [ ] **Step 2: Add axe-core install rule**

```markdown
**Accessibility scaffolding (v3.1+).** If PRD declares `WCAG-Level: A | AA |
AAA`, scaffolding includes:

- `package.json` devDependencies: `@axe-core/playwright`, `axe-core`
- `tests/e2e/<NNN-feature-name>/journey.spec.ts` imports:

      import AxeBuilder from '@axe-core/playwright';

If `WCAG-Level: N/A`, skip — no axe-core install needed.
```

- [ ] **Step 3: Verify**

Grep for `@axe-core/playwright` — expect 2-3 matches.

- [ ] **Step 4: Commit**

```
git add plugins/harness/agents/generator.md
git commit -m "feat(harness/v3.1/G2): Generator BUILD scaffolds axe-core when WCAG declared"
```

---

### Task 5: axe-core a11y — SIMULATE Step 3 per-row scan (G2 / B4 part C)

**Files:** Modify `plugins/harness/agents/generator.md`

- [ ] **Step 1: Locate SIMULATE Step 3**

Grep for `#### Step 3: Drive State-Transition AC rows`. Locate end of existing 3-bullet a/b/c drive procedure, before any "On any failure" paragraph.

- [ ] **Step 2: Insert per-row a11y scan**

```markdown
**Per-row accessibility scan (v3.1+).** After driving each State-Transition
row's user flow but before recording the verdict, run axe-core analysis:

    import AxeBuilder from '@axe-core/playwright';

    const wcagLevel = process.env.WCAG_LEVEL ?? 'AA';
    const tags = wcagLevel === 'AAA' ? ['wcag2a','wcag2aa','wcag2aaa'] :
                 wcagLevel === 'A'   ? ['wcag2a'] :
                 wcagLevel === 'AA'  ? ['wcag2a','wcag2aa'] :
                 null;

    if (tags) {
      const axeResults = await new AxeBuilder({ page }).withTags(tags).analyze();
      // record per-row a11y verdict per impact severity
    }

Per-row a11y verdict rules:
- For each violation with `impact = serious | critical`: row's `a11y` column
  = ❌ → treat parent FR as Partial (same as DB ❌ rule for State-Transition rows)
- For each violation with `impact = moderate`: row's `a11y` column = ⚠️ →
  records under Major findings; doesn't fail Part A
- For each violation with `impact = minor`: information-only; logs in
  simulation-report's `## A11y informational` aggregated section

Record the new `a11y` column in simulation-report.md per-state-transition
table. If WCAG-Level is N/A, skip this scan and emit `a11y: N/A — non-UI feature`.

Per-rule disable allowed via `axe.run({rules: { 'rule-id': {enabled: false}}})`
but requires ADR justification in `progress/decisions.md`.
```

- [ ] **Step 3: Verify**

Grep for `AxeBuilder` — expect 4+ matches.

- [ ] **Step 4: Commit**

```
git add plugins/harness/agents/generator.md
git commit -m "feat(harness/v3.1/G2): SIMULATE Step 3 axe-core per-state-transition scan"
```

---

### Task 6: axe-core a11y — Evaluator + simulation-report template (G2 / B4 part D)

**Files:** Modify `plugins/harness/templates/features/simulation-report.md.txt`, `plugins/harness/templates/evaluator/criteria.md.txt`

- [ ] **Step 1: Add a11y column to simulation-report template**

In the `## Per-state-transition verification` table, change column header from:

    | Entity.Field | From | To | Test name | DB confirmed | UI confirmed |

To:

    | Entity.Field | From | To | Test name | DB confirmed | UI confirmed | a11y |

Update example rows correspondingly. Add new optional section after `## Cross-runtime parity`:

```markdown
---

## A11y informational (v3.1+, optional)

<emit only if any State-Transition row produced minor-impact a11y violations>

Aggregated minor-severity axe findings (informational; not gating):

| Rule | Count | Sample location |
|------|-------|-----------------|
| <rule-id> | N | <selector> |
```

- [ ] **Step 2: Promote criteria.md.txt:138 to first-class enforcement**

Find existing prose mention of axe-core (around line 138). Replace with:

```markdown
**Accessibility (NFR-driven, v3.1+):**

If PRD declares `WCAG-Level: A | AA | AAA` (in `prd.md` § Accessibility NFR):

- axe-core violations from simulation-report.md `a11y` column with
  `impact = serious | critical` count toward Part A FAIL (treats parent
  FR as Partial, same gating rule as DB ❌)
- `impact = moderate` violations are MAJOR findings (do not fail Part A)
- `impact = minor` violations are informational only (logged in
  `## A11y informational` section of simulation-report)
- Per-rule disable allowed via project's `axe.run({rules: { 'rule-id':
  {enabled: false}}})` config, but requires ADR justifying the disable

If WCAG-Level: N/A or not declared, skip a11y gating entirely.
```

- [ ] **Step 3: Verify**

Grep `simulation-report.md.txt` for `a11y` — expect 4+ matches.

- [ ] **Step 4: Commit**

```
git add plugins/harness/templates/features/simulation-report.md.txt plugins/harness/templates/evaluator/criteria.md.txt
git commit -m "feat(harness/v3.1/G2): a11y column in simulation-report + criteria.md first-class enforcement"
```

---

### Task 7: superpowers code-reviewer — Evaluator Step 3 split + 3a integration (G3 / D3 part A)

**Files:** Modify `plugins/harness/agents/evaluator.md`

- [ ] **Step 1: Locate current Step 3**

Grep for `### Step 3: Code Quality Review`. Read the entire Step 3 body.

- [ ] **Step 2: Restructure into 3a + 3b + 3c**

Replace the existing Step 3 block with:

```markdown
### Step 3: Code Quality Review (v3.1+)

Step 3 splits into three sub-steps. Run them in order: 3a → 3b → 3c.
After 3a (mechanical second-opinion via code-reviewer), 3b and 3c MUST
still run regardless of code-reviewer's verdict — anti-leniency protocol
applies to YOUR judgment, not code-reviewer's.

#### Step 3a: Mechanical second-opinion via superpowers:code-reviewer (v3.1+)

If `superpowers` plugin is installed, dispatch a Task subagent using the
code-reviewer skill template:

- Tool: Agent (general-purpose)
- Subagent type: superpowers:code-reviewer
- Inputs to provide in the prompt:
  - `WHAT_WAS_IMPLEMENTED`: implementation-report.md `## FR → Implementation Map` summary
  - `PLAN_OR_REQUIREMENTS`: full path `.harness/features/${FEATURE}/contract.md`
  - `BASE_SHA`: `git rev-parse main` (sprint-start anchor — capture before dispatch)
  - `HEAD_SHA`: `git rev-parse harness/build/${FEATURE}` (current build branch HEAD)
  - `DESCRIPTION`: one-paragraph summary of the feature's purpose

**Cost gate:** if the diff is small (`git diff --stat ${BASE_SHA}..${HEAD_SHA} | wc -l` < 50),
skip Step 3a entirely. Document the skip in eval-report.md as
"Step 3a: skipped — diff < 50 lines (cost gate)".

**Output translation rules (anti-leniency preservation, MANDATORY):**

When parsing code-reviewer's response:
- code-reviewer's `## Critical (Must Fix)` items → eval-report.md
  `## Critical Findings` (severity: CRITICAL)
- code-reviewer's `## Important (Should Fix)` items → eval-report.md
  `## Major Findings` (severity: MAJOR)
- code-reviewer's `## Minor` items → eval-report.md `## Minor Findings`
  (severity: minor)
- **DROP code-reviewer's `## Strengths` section verbatim.** Do NOT
  propagate "what was done well" prose — adversarial framing is
  preserved by NOT importing collaborative-skeptic narrative.
- **DROP code-reviewer's `## Assessment` section verbatim.** (E.g., "Ready
  to merge: With fixes" — Evaluator computes its own verdict in Step 7.)
- code-reviewer is a SECOND OPINION, not a replacement. Run Steps 3b and
  3c after 3a, regardless of how thoroughly code-reviewer reviewed.

If `superpowers` plugin NOT installed:
- Skip Step 3a; doctor warned at MAJOR
- Proceed directly to Step 3b
- Document the skip in eval-report.md as "Step 3a: skipped — superpowers
  plugin not installed"

#### Step 3b: Manual constitutional review

[existing v3.0 Step 3 manual prose unchanged — file-length, function-length,
error-handling, imports, naming. KEEP this content verbatim.]

#### Step 3c: Constitutional grep scans

[existing v3.0 Step 3 grep prose unchanged — `: any`, `console.log`, lint,
catch-block ban from v3.0 G4. KEEP this content verbatim.]
```

(The Edit replaces ONLY the heading "### Step 3: Code Quality Review" with the restructured preamble + new Step 3a sub-section. Steps 3b and 3c become re-labels of the existing Step 3 content split at appropriate boundaries.)

- [ ] **Step 3: Verify the structural split**

Grep for `^#### Step 3a` — expect 1 match. `^#### Step 3b` — 1 match. `^#### Step 3c` — 1 match. `code-reviewer` — 5+ matches.

- [ ] **Step 4: Commit**

```
git add plugins/harness/agents/evaluator.md
git commit -m "feat(harness/v3.1/G3): Evaluator Step 3 splits 3a (code-reviewer) + 3b (manual) + 3c (grep)"
```

---

### Task 8: superpowers code-reviewer — SKILL.md + doctor warning (G3 / D3 part B)

**Files:** Modify `plugins/harness/skills/harness/SKILL.md`, `plugins/harness/commands/doctor.md`

- [ ] **Step 1: Annotate superpowers in Optional Plugins table**

Find `superpowers` row in Optional Plugins table. Replace with:

```markdown
| superpowers | Generator BUILD (TDD), Evaluator EVALUATE Step 3a (v3.1+ — code-reviewer integration), systematic-debugging | TDD skill, code-reviewer skill, debugging skill | /plugin install superpowers@claude-plugins-official |
```

- [ ] **Step 2: Upgrade doctor warning level**

Find existing superpowers check in `commands/doctor.md`. Replace with:

```markdown
**superpowers plugin (v3.1+ — soft-required for Evaluator Step 3a)**

Check whether `superpowers` plugin is installed:

    ls ~/.claude/plugins/cache/claude-plugins-official/superpowers/ 2>/dev/null

- If installed: print `superpowers detected (Evaluator Step 3a will dispatch code-reviewer)`.
- If missing: print MAJOR warning:

  ⚠️  MAJOR: superpowers plugin not installed.

    Evaluator Step 3a (v3.1+ code-reviewer integration) will be skipped.
    This is a soft requirement — the harness still functions without it,
    falling back to v3.0's Step 3 (manual review + grep scans only).

    To install:
      /plugin install superpowers@claude-plugins-official

Do NOT block sprint dispatch on missing superpowers. Doctor exits 0;
warning is informational.
```

- [ ] **Step 3: Verify**

Grep `SKILL.md` for `code-reviewer integration` — 1 match. Grep `doctor.md` for `MAJOR.*superpowers` — 1 match.

- [ ] **Step 4: Commit**

```
git add plugins/harness/skills/harness/SKILL.md plugins/harness/commands/doctor.md
git commit -m "feat(harness/v3.1/G3): SKILL.md + doctor.md annotate superpowers v3.1+ requirement"
```

---

## Phase 2 — Tier 2 (substantive verification)

### Task 9: Stryker mutation — Generator BUILD scaffolding (G4 / B1 part A)

**Files:** Modify `plugins/harness/agents/generator.md`

- [ ] **Step 1: Locate scaffolding section (next to axe-core scaffolding from T4)**

- [ ] **Step 2: Add Stryker scaffolding rule after axe scaffolding**

```markdown
**Mutation testing scaffolding (v3.1+).** When scaffolding (Phase 1 of BUILD),
if source size ≥ 100 LoC:

- `package.json` devDependencies: `@stryker-mutator/core`, `@stryker-mutator/vitest-runner`
- Write `stryker.config.json` at project root:

      {
        "$schema": "https://raw.githubusercontent.com/stryker-mutator/stryker-js/master/packages/core/schema/stryker-schema.json",
        "testRunner": "vitest",
        "incremental": true,
        "thresholds": { "high": 80, "low": 60, "break": null },
        "mutate": ["src/**/*.ts", "!src/**/*.spec.ts", "!src/**/*.test.ts"]
      }

If source < 100 LoC, skip — mutation overhead exceeds value. Document in
implementation-report.md `## Setup required`: "Mutation testing skipped —
project below 100 LoC threshold".
```

- [ ] **Step 3: Verify**

Grep for `@stryker-mutator/core` — expect 1-2 matches.

- [ ] **Step 4: Commit**

```
git add plugins/harness/agents/generator.md
git commit -m "feat(harness/v3.1/G4): Generator BUILD scaffolds Stryker config when ≥100 LoC"
```

---

### Task 10: Stryker mutation — NEGOTIATE per-FR target + Evaluator gate (G4 / B1 part B)

**Files:** Modify `plugins/harness/agents/generator.md`, `plugins/harness/agents/evaluator.md`, `plugins/harness/templates/features/contract.md.txt`

- [ ] **Step 1: Add NEGOTIATE per-FR mutation-score-target**

After NEGOTIATE's existing `Populate State-Transition + Negative-Path test names` sub-step:

```markdown
**Mutation score target (v3.1+).** For each FR, declare a mutation-score
target in the proposal:
- 70% — critical paths (auth, payment, data-mutation, security-sensitive)
- 50% — standard FRs (CRUD, validation, formatting)
- N/A — non-testable FRs (config-only, type-only, documentation)

Target appears in proposal.md `## Mutation Score Targets` table:

    | FR | Target | Rationale |
    |----|--------|-----------|
    | FR-001 | 70% | auth-critical |
    | FR-002 | 50% | standard CRUD |
    | FR-003 | N/A | type-only refactor |

NEGOTIATE commits to scoring; BUILD writes tests aiming for the target;
Evaluator Step 4 gates on per-FR achievement.
```

- [ ] **Step 2: Add mutation gate to Evaluator Step 4**

After existing `npx vitest run` step in Step 4:

```markdown
**Mutation gate (v3.1+).** If `stryker.config.json` exists at project root,
run mutation testing per contract's per-FR scoping:

    npx stryker run --incremental --mutate "src/<feature-folder-paths>/"

Wait for completion. Parse `reports/mutation/mutation.json`:

- For each FR with declared target X% in contract `## Mutation Score Targets`:
  - If `mutationScore` < X% by ≥10 points → MAJOR finding ("FR-NNN
    mutation score Y% < target X%; survived mutants suggest assertion
    weakness")
  - If `mutationScore` < 30% on any per-FR module → CRITICAL finding
  - For FRs with target = N/A, skip mutation check
- Equivalent mutants (annotated by Stryker as `EquivalentMutant`) are
  NOT counted against Generator. To exclude: project author adds
  `excludedMutations` to stryker.config.json with ADR-justified rationale.

If Stryker errors out (cannot run incremental DB, missing config, vitest
config incompatible), this is **infrastructure failure NOT test failure**
— file `## Mutation Infrastructure Failure` finding under Code Quality
and proceed; do NOT block Part A on Stryker unavailability.
```

- [ ] **Step 3: Add Mutation Score Target section to contract template**

After existing `## State-Transition ACs` and `## Negative-Path Coverage`:

```markdown
## Mutation Score Targets (v3.1+)

For each FR, declare a mutation-score target. Generator NEGOTIATE populates;
BUILD writes tests aiming for the targets; Evaluator Step 4 gates on
per-FR achievement.

| FR | Target | Rationale |
|----|--------|-----------|

<!-- Example row — DELETE before delivery; replace with one row per FR -->
| <example: FR-001> | 70% | <example: auth-critical path> |

If this feature has only N/A FRs, write: `_None — all FRs are non-testable
(target = N/A)._`
```

- [ ] **Step 4: Verify**

Grep `agents/generator.md` for `Mutation score target` — 2+ matches. `agents/evaluator.md` for `Mutation gate` — 1 match. `contract.md.txt` for `## Mutation Score Targets` — 1 match.

- [ ] **Step 5: Commit**

```
git add plugins/harness/agents/generator.md plugins/harness/agents/evaluator.md plugins/harness/templates/features/contract.md.txt
git commit -m "feat(harness/v3.1/G4): NEGOTIATE per-FR mutation target + Evaluator Step 4 mutation gate"
```

---

### Task 11: fast-check property tests — NEGOTIATE eligibility + BUILD writing (G5 / B2)

**Files:** Modify `plugins/harness/agents/generator.md`, `plugins/harness/agents/evaluator.md`, `plugins/harness/templates/features/proposal.md.txt`

- [ ] **Step 1: Add NEGOTIATE property-eligibility column**

After Mutation Score Targets sub-step (T10):

```markdown
**Property-test eligibility (v3.1+).** For each FR, declare:

- `Y` — pure functions, parsers, validators, sort/filter/aggregate, data transformations
- `N` — UI flows, single-shot integration paths, side-effect-heavy I/O
- `Race` — FRs with concurrent state mutations; use `fc.scheduler` for
  race-condition detection (e.g., shared counter, mutex protection)

One-sentence reason per FR. Eligibility appears in proposal.md
`## Property-test Eligibility` table:

    | FR | Eligible? | Reason |
    |----|-----------|--------|
    | FR-001 | Y | parses CSV input, edge cases matter |
    | FR-002 | N | UI flow (login form), example tests sufficient |
    | FR-003 | Race | shared counter increment, concurrency test required |
```

- [ ] **Step 2: Add BUILD Phase 2 property-test rule**

After per-FR fixtures.ts commit rule:

```markdown
**Property-test write rule (v3.1+).** For FRs flagged property-eligible
(Y or Race) in NEGOTIATE, write at least one property test alongside
example tests in the SAME atomic per-FR commit. Use `@fast-check/vitest`:

    import { test, expect } from 'vitest';
    import { test as testProp } from '@fast-check/vitest';
    import * as fc from 'fast-check';

    testProp.prop([fc.array(fc.integer())])('sortAscending preserves length', (arr) => {
      expect(sortAscending(arr).length).toBe(arr.length);
    });

For `Race` flagged FRs, use `fc.scheduler()` for concurrent operation testing:

    testProp.prop([fc.scheduler()])('counter increment is atomic', async (s) => {
      const counter = new Counter();
      await Promise.all([
        s.scheduleFunction(counter.increment.bind(counter))(),
        s.scheduleFunction(counter.increment.bind(counter))(),
      ]);
      await s.waitAll();
      expect(counter.value).toBe(2);
    });

**Constraints:**
- `numRuns` ≥ 100 (default; never reduce to 1)
- No hardcoded `seed: <fixed>` — let fast-check shrinking find
  reproducible cases
- Properties must assert behavior, not type-shape
- Property tests run via Vitest in Evaluator Step 4
```

- [ ] **Step 3: Add proposal template column**

In `templates/features/proposal.md.txt`, add:

```markdown
## Property-test Eligibility (v3.1+)

For each FR, declare property-eligibility per Generator NEGOTIATE rule.

| FR | Eligible? | Reason |
|----|-----------|--------|

<!-- Example row — DELETE before delivery -->
| <example: FR-001> | Y | <example: parses CSV input, edge cases matter> |
```

- [ ] **Step 4: Add Evaluator Step 4 trivial-property check**

After Mutation gate paragraph (T10):

```markdown
**Property-test triviality check (v3.1+).** When property tests exist
(grep test files for `fc.assert(fc.property` or `testProp.prop`), verify:

- `numRuns` ≥ 100 (if explicitly reduced to 1-10, MAJOR)
- No hardcoded `seed:` parameter (if found, MAJOR)
- Properties assert behavior, not type-shape (grep for property bodies
  containing ONLY `typeof` checks or `instanceof` without other
  assertions; flag these as MAJOR — trivially-true properties)
```

- [ ] **Step 5: Verify**

Grep `agents/generator.md` for `@fast-check/vitest` — 2 matches. `proposal.md.txt` for `## Property-test Eligibility` — 1 match. `agents/evaluator.md` for `Property-test triviality` — 1 match.

- [ ] **Step 6: Commit**

```
git add plugins/harness/agents/generator.md plugins/harness/agents/evaluator.md plugins/harness/templates/features/proposal.md.txt
git commit -m "feat(harness/v3.1/G5): NEGOTIATE property eligibility + BUILD property tests + Evaluator triviality check"
```

---

### Task 12: MADR ADR matrix — Planner Pass 2 architecture template (G6 / C4 part A)

**Files:** Modify `plugins/harness/agents/planner.md`

- [ ] **Step 1: Locate Pass 2 ADR template**

Grep for `### ADR-NNN:` or existing ADR template prose.

- [ ] **Step 2: Replace ADR template with MADR matrix structure**

```markdown
### ADR-NNN: <decision>

**Status**: proposed | accepted | superseded

**Decision drivers** (≥3, ≥2 must be NFR-NNN refs):
- <driver-1> (e.g., NFR-001: response p95 < 200ms)
- <driver-2> (e.g., NFR-003: zero-downtime deploys)
- <driver-3> (e.g., team familiarity, license compatibility)

**Considered options** (≥2; each Context7-verified to exist):
- <option-A>
- <option-B>
- <option-C>

For EACH option above, run `mcp__context7__resolve-library-id` (or the
plugin-namespaced equivalent) to verify the option exists. If Context7
returns nothing for an alleged option, drop it as hallucination. DO NOT
list options you cannot verify.

**Pros and cons matrix:**

| Option | <driver-1> | <driver-2> | <driver-3> | Score |
|--------|------------|------------|------------|-------|
| <A>    | <eval>     | <eval>     | <eval>     | N/M   |
| <B>    | <eval>     | <eval>     | <eval>     | N/M   |
| <C>    | <eval>     | <eval>     | <eval>     | N/M   |

Score per cell: 0 (fails driver), 1 (meets driver), 2 (exceeds driver).
Total score = sum across drivers; M = total possible (drivers × 2).

**Chosen**: <option-X>

**Why**: <one sentence pointing to highest-scoring driver where chosen
option won>

**Hallucination check**: Context7 query results for non-chosen alternatives
confirming they exist (file:line references, NOT memory-recall):
- <option-Y>: Context7 lookup succeeded — `<library-id>` resolved
- <option-Z>: Context7 lookup succeeded — `<library-id>` resolved

**Affects**: <list of FRs / files / future ADRs depending on this decision>
```

- [ ] **Step 3: Update RED FLAGS row 5**

Find row containing `I'll pick the framework I know best`. Replace its `What to do instead` cell with:

```markdown
Compare ≥2 options. **Run Context7 `resolve-library-id` for EACH alternative; if Context7 returns nothing for an alleged option, it's a hallucination — drop it.** Score per-driver. Justify highest-scoring choice in the ADR `Why` line.
```

- [ ] **Step 4: Verify**

Grep `planner.md` for `Pros and cons matrix` — 1 match. `Hallucination check` — 1 match. `Context7 lookup succeeded` — 2 matches (in template).

- [ ] **Step 5: Commit**

```
git add plugins/harness/agents/planner.md
git commit -m "feat(harness/v3.1/G6): Planner Pass 2 ADR template uses MADR matrix with Context7 verification"
```

---

### Task 13: MADR ADR matrix — V8b self-validation + analyze warning (G6 / C4 part B)

**Files:** Modify `plugins/harness/agents/planner.md`, `plugins/harness/commands/analyze.md`

- [ ] **Step 1: Add V8b row to self-validation checklist**

After V18 (the existing v3.0 last row):

```markdown
- [ ] V8b: Every architecture.md ADR with `Considered options` has
      ≥2 alternatives, ≥3 decision drivers (≥2 NFR-NNN refs), per-driver
      score per option, AND Context7 verification reference for each
      non-chosen alternative. Chosen option's `Why` line explicitly
      references the highest-scoring driver.
```

- [ ] **Step 2: Bump self-validation header from 18-point to 19-point**

Grep for `18-point self-validation` (header + closing summary). Replace BOTH with `19-point self-validation`. Also update RED FLAGS table reference.

- [ ] **Step 3: Add /harness:analyze warning check**

In `commands/analyze.md` `Tech stack conflicts` sub-step, append:

```markdown
**ADR matrix completeness check (v3.1+, WARNING).** For each ADR in
architecture.md:

- If the ADR has a `Considered options` field with only one option (or
  none), emit WARNING (NOT CRITICAL — preserves existing-project
  compatibility): "ADR-NNN has fewer than 2 considered options; v3.1+
  requires MADR matrix structure."
- If the ADR has a `Pros and cons matrix` but missing the
  `Hallucination check`, emit WARNING: "ADR-NNN matrix missing Context7-
  verification of non-chosen alternatives."

These are WARNINGs not CRITICAL because they don't break the pipeline;
they surface drift the user can choose to address via `/harness:edit`.
```

- [ ] **Step 4: Verify**

Grep `planner.md` for `V8b` — 1 match. `19-point` — 2+ matches. `analyze.md` for `MADR matrix structure` — 1 match.

- [ ] **Step 5: Commit**

```
git add plugins/harness/agents/planner.md plugins/harness/commands/analyze.md
git commit -m "feat(harness/v3.1/G6): V8b self-validation + analyze.md ADR matrix warning"
```

---

## Phase 3 — Tier 3 (opportunistic + documentation)

### Task 14: size-limit bundle budget — Planner + Generator + Evaluator (G7 / B6 partial)

**Files:** Modify `plugins/harness/agents/planner.md`, `plugins/harness/agents/generator.md`, `plugins/harness/agents/evaluator.md`, `plugins/harness/templates/features/contract.md.txt`

- [ ] **Step 1: Add Planner Pass 1 bundle-size question**

After `Sub-step W: Accessibility conformance` (T3):

```markdown
**Sub-step BS: Bundle-size budget (v3.1+, UI projects only)**

When the project has client-side JavaScript output (architecture declares
a UI layer with build artifacts), ask via AskUserQuestions:

> "Bundle-size budget for production builds?
>   1. Strict (≤ 150kB per chunk, ≤ 500kB total)
>   2. Standard (≤ 250kB per chunk, ≤ 1MB total)
>   3. Custom (specify per-chunk + total)
>   4. None (skip — early prototype, no budget)"

Record in `prd.md` under `## Bundle-size NFR`:

    bundle_size_per_chunk: "150kB" | "250kB" | "<custom>"
    bundle_size_total: "500kB" | "1MB" | "<custom>"

If `None`: SIMULATE/Evaluator skip bundle-size gate. If declared:
Generator BUILD scaffolds `.size-limit.json` from these values.

Skip this sub-step if architecture declares no client-side JS output.
```

- [ ] **Step 2: Add Generator BUILD size-limit scaffolding**

After Mutation testing scaffolding (T9):

```markdown
**Bundle-size scaffolding (v3.1+).** When PRD declares Bundle-size NFR
(non-`None`), scaffolding includes:

- `package.json` devDependencies: `size-limit`, `@size-limit/preset-app`
- `package.json` scripts: `"size": "size-limit"`
- Write `.size-limit.json` at project root, populated from PRD values:

      [
        { "path": ".next/static/chunks/*.js", "limit": "<per-chunk>" },
        { "path": ".next/static/**/*.{js,css}", "limit": "<total>" }
      ]

(Adjust `path` patterns for the actual stack — Next.js, Vite, Remix have
different output locations. Generator BUILD reads architecture.md's
declared stack to choose the right paths.)

If `Bundle-size NFR: None` declared, skip this scaffolding.
```

- [ ] **Step 3: Add Evaluator Step 4 size-limit gate**

After Mutation gate (T10):

```markdown
**Bundle-size gate (v3.1+).** If PRD declares Bundle-size NFR (non-`None`)
and project has `.size-limit.json`, run:

    npx size-limit

Parse output:
- If any path's measured size ≤ budget → `## Bundle Size` section in
  eval-report logs success
- If any path exceeds budget → MAJOR finding (deterministic — bundle
  size is operational concern, not Part A contract-compliance gate)
- If size-limit errors out → infrastructure failure: file `## Bundle-size
  Infrastructure Failure` finding; do NOT block Part A on size-limit
  unavailability.
```

- [ ] **Step 4: Add Bundle-size Budget section to contract template**

After Mutation Score Targets section (T10):

```markdown
## Bundle-size Budget (v3.1+, UI projects only)

If PRD declares Bundle-size NFR, populate this section. If PRD declares
`None`, write: `_None — early prototype, no budget enforced._`

    bundle_size_per_chunk: <from PRD>
    bundle_size_total: <from PRD>

Generator BUILD scaffolds `.size-limit.json` from these values. Evaluator
Step 4 asserts `npx size-limit` against the budget.
```

- [ ] **Step 5: Verify**

Grep `planner.md` for `Bundle-size budget` — 2+ matches. `generator.md` for `size-limit` — 3+ matches. `evaluator.md` for `Bundle-size gate` — 1 match. `contract.md.txt` for `## Bundle-size Budget` — 1 match.

- [ ] **Step 6: Commit**

```
git add plugins/harness/agents/planner.md plugins/harness/agents/generator.md plugins/harness/agents/evaluator.md plugins/harness/templates/features/contract.md.txt
git commit -m "feat(harness/v3.1/G7): size-limit bundle budget — Planner NFR + Generator scaffold + Evaluator gate"
```

---

### Task 15: Refactor pattern stub — Generator RED FLAG + SKILL.md docs (G8 / D2 stub)

**Files:** Modify `plugins/harness/agents/generator.md`, `plugins/harness/skills/harness/SKILL.md`

- [ ] **Step 1: Update Generator RED FLAGS row 7**

Grep for `While I'm here, I'll also refactor that adjacent code`. Replace the row's `What to do instead` cell with:

```markdown
One contract = one set of changes. **Refactors live in their own contract** — use `/harness:quick` with a refactor-shaped contract template (see SKILL.md § Refactor Pattern). Adjacent cleanup goes in `known-issues.md` for a later sprint OR future `/harness:refactor` (v3.2+, when usage data justifies the dedicated command).
```

- [ ] **Step 2: Add SKILL.md Refactor Pattern section**

After `## Worker Readiness Patterns (v3.1+)` (T1):

```markdown
## Refactor Pattern (v3.1+)

Behavior-preserving cross-cutting changes (rename, extract, deduplicate,
homogenize) don't fit the FR/AC-driven sprint contract shape. Until the
dedicated `/harness:refactor` command lands (v3.2+), express refactor
work via `/harness:quick` with this contract template.

### Contract template

    ## Refactor: <intent in one sentence>

    **Mode**: REFACTOR (behavior-preserving, no new tests, no new behavior)

    ### Behavior preservation invariant (the AC)
    - AC-R1: All existing tests pass before and after (`vitest run` + `playwright test` exit 0)
    - AC-R2: Git diff shows ZERO behavior changes — only renames/moves/extractions
    - AC-R3: No new dependencies, no new exports, no API surface changes

    ### Affected sites (user-grepped)
    - `src/path/a.ts:42` — `getUser()` → `getCurrentUser()`
    - `src/path/b.ts:118` — same rename
    - `src/path/c.ts:201` — same rename

    ### Out of scope (≥3 explicit non-changes)
    - Test logic changes
    - New feature additions
    - Adjacent bug fixes (file in known-issues.md)

    ### Definition of Done
    - All affected sites updated atomically (single commit)
    - ALL pre-existing tests pass; no test changes
    - No new tests added
    - Constitution scan still clean

### Workflow

1. User invokes `/harness:quick "<refactor intent>"` with the template
   above as the contract body.
2. Generator BUILD reads contract, refactors per scope, runs existing
   tests. Generator's RED FLAGS row 7 prose explicitly directs refactor-
   shaped contracts to this pattern.
3. Evaluator EVALUATE confirms binary AC: tests pass + diff is rename-only
   + no new dependencies.
4. PASS → squash-merge with `[harness:refactor] <intent>` commit.

### Why this pattern (not /sprint)

- Refactors have no PRD/spec authoring step — `/quick` skips Planner correctly
- Refactors have binary ACs (tests pass / diff is rename-only) — no
  numeric Part B grading needed
- Refactors are atomic — single commit, single eval pass

### When to escalate to /harness:refactor (v3.2+)

If the refactor is large (≥10 files affected) OR requires architectural
judgment (extract module boundary, change paradigm), defer to v3.2's
dedicated command. Track usage in `known-issues.md` to inform v3.2 demand.

### Tracking convention

When invoking /quick for refactor work, prepend the description with
`[refactor]`:

    /harness:quick "[refactor] rename getUser to getCurrentUser everywhere"

This makes the pattern grep-able for v3.2 demand-data analysis.
```

- [ ] **Step 3: Verify**

Grep `agents/generator.md` for `Refactors live in their own contract` — 1 match. `SKILL.md` for `## Refactor Pattern` — 1 match.

- [ ] **Step 4: Commit**

```
git add plugins/harness/agents/generator.md plugins/harness/skills/harness/SKILL.md
git commit -m "feat(harness/v3.1/G8): Refactor pattern stub — Generator RED FLAG + SKILL.md docs (no new command)"
```

---

## End of Part 1

Phase 1-3 implementation complete after Tasks 1-15.

**Continue to Part 2** (`docs/superpowers/plans/2026-05-08-belcort-v3.1-verification-augmentation-part-2.md`) for:

- Phase 4 documentation updates (Tasks 16-20) — anthropic-alignment.md / CHANGELOG.md / ROADMAP.md / README.md / version bumps
- Phase 5 release (Tasks 21-23) — pre-push audit + canary on BELCORT ACC v2 + push to release/v3.1.0 + tag
- Self-Review against spec coverage
- Execution handoff (Subagent-Driven vs Inline)

Do NOT begin documentation tasks until all 15 implementation tasks have committed cleanly.
