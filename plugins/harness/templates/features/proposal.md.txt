<!-- BELCORT Harness — Template: proposal.md -->
<!--
Written by: Generator in NEGOTIATE mode.
Location when instantiated: .harness/features/{current-feature}/proposal.md
Purpose: Generator's HOW-to-implement proposal, reviewed by Evaluator before any code is written.
Canonical source: this file. The inline copy in plugins/harness/agents/generator.md must stay in sync.
-->

# Implementation Proposal — Round {{N}}

**Date**: {{ISO_DATE}}
**Round**: {{N}} of max 3

## Component/Module Breakdown
- {{component_name}} — {{one_line_responsibility}}
- {{component_name}} — {{one_line_responsibility}}

## Directory Structure

<!--
v2.2-staging+ (FIX B1): the Generator fills this section based on the project's
DETECTED layout, not a hard-coded src/ tree. Read `.harness/manifest.yaml` →
`project.src_dir` and `project.stack_family` first:

  - Next.js (app-router): src_dir = "app". Add new pieces UNDER app/ and lib/,
    NOT a parallel src/. e.g., `app/(marketing)/feature-x/page.tsx`,
    `lib/feature-x/`.
  - Vite/CRA/Remix:        src_dir = "src". Use src/features/, src/lib/.
  - Flat layout:           src_dir = ".".  Mirror existing top-level conventions.
  - Python/Rust/Go:        follow the language's idiomatic layout, NOT React.

The example block below shows the React/Vite default. Replace it with the
ACTUAL tree your proposal targets. Brownfield: propose ADDITIVE additions to
the existing tree, never a parallel src/ alongside an existing app/ — that
creates a double-tree the Evaluator will flag as architecture drift.
-->

```
{{src_dir}}/
├── {{additive_dir_1}}/
│   ├── {{file_1}}
│   └── {{file_2}}
└── {{additive_dir_2}}/
```

## Data Model Proposal

| Entity | Fields | Serves FR |
|--------|--------|-----------|
| {{Entity}} | {{field_list}} | {{FR_ids}} |

## API Surface Proposal

| Endpoint | Method | Purpose | Serves FR |
|----------|--------|---------|-----------|
| {{path}} | {{METHOD}} | {{one_line}} | {{FR_ids}} |

## FR → Implementation Mapping

| FR | Components touched | Files (planned) | Test strategy |
|----|-------------------|-----------------|---------------|
| {{FR-001}} | {{component}} | {{file_list}} | {{unit_e2e_mix}} |

## AC → Test Approach

| AC | How I'll verify it in BUILD mode |
|----|----------------------------------|
| {{AC-001-1}} | {{runner_and_assertion}} |

## Risk Flags (things I'm uncertain about)
- {{area_uncertain}}
- {{edge_case_draft_contract_missed}}
- {{dependency_unverified_via_context7}}

## Context7 Lookups Performed

<!--
v2.2+ MANDATORY section. Every framework-specific API referenced anywhere in this proposal
(Component Breakdown, Data Model, API Surface, FR→Implementation Mapping, AC→Test Approach)
MUST appear in this table with a real Context7 lookup result. The architecture.md Context7
Verification Log already covers Planner-named libraries at the stack level — this table
adds the API-level / feature-level lookups you performed during NEGOTIATE.

Empty table = REVIEW-PROPOSAL flags `R-NN: Context7 lookups missing or empty` and `needs-revision`.
Generic entries ("React APIs") = `R-NN: lookups too coarse to be useful` and `needs-revision`.
-->

| API / library feature | Context7 ID | Lookup query | Verified? | Used in proposal section |
|-----------------------|-------------|--------------|-----------|--------------------------|
| {{React Server Components stable APIs}} | {{/facebook/react}} | {{"Server Components in React 19"}} | {{yes}} | {{Component Breakdown — FR-002}} |
| {{Drizzle select with multiple where conditions}} | {{/drizzle-team/drizzle-orm}} | {{"select where AND conditions"}} | {{yes}} | {{Data Model Proposal}} |

## Questions for Evaluator
- {{clarifying_question}}

## If Round 2+: Response to Previous Review

| Evaluator ask | How I addressed it |
|---------------|--------------------|
| {{from_review_md}} | {{change_to_proposal}} |
